# Campus Guinness — 项目初始化

已在 `E:/campus-guinness` 下创建项目骨架。

快速初始化（WSL / Linux / macOS）:

```
cd E:/campus-guinness
./scripts/init_repo.sh
```

快速初始化（Windows PowerShell）:

```
cd E:/campus-guinness
scripts\init_repo.ps1
```
